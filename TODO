[X] volumes
[X] ssl
[X] PID 1
[X] differrence between docker  container and docker compose container


[X]ports:
    - 443:443
    host container

[X] forground and background

[X] NOTE: Without exec, your script is PID 1, and the real app is a child — it won’t receive signals directly.

[X] network

chown -R www-data:www-data /var/www/html -> change ownership
the first www-data refers to the user
the second www-data refers to the group.

```
[X] compose 
[X] docker-network 
[X] volume 
[ ] ssl digram

```
-nodes: Skips the option to encrypt the private key.

pem Privacy Enhanced Mail